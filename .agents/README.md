# Agent Workspace

This directory is the project-local contract for coding agents. Load `AGENTS.md`, then the applicable skill and spec before editing.

Workflow: `spec -> test -> domain -> application -> adapters -> verify -> docs`.
