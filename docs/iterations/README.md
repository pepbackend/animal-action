# Iteration Log

Add one dated note per meaningful development iteration using `YYYY-MM-DD-<short-name>.md`. Record the request, context read, decisions made, files changed, tests run, known limitations, and next actions. Keep entries concise and link the relevant spec and ADR.
