# Local Development

Run `./start.sh` to build and start PostgreSQL, Kafka, and the application. Run `./stop.sh` to stop containers. Use `docker compose ps` for status and `docker compose logs postgres kafka` for infrastructure logs. Do not use production credentials in local configuration.
