# Product CRUD

This legacy template specification is retained as historical context. The active feature specification is `.specs/animal-action.md`.

The project now follows the API/event-contract-first and TDD workflow defined in `AGENTS.md`.
